1.模型返回信息只展示<think>,没有按照思考的隐藏，模型正常输出的展示要求进行展示。
2.工具调用信息不可见。无法知道模型调用了哪些工具，有没有调用，调用结果等。
3.评估脑工作过程不可见，不知道评估脑怎么评估，评估结果是什么。
4.记忆脑插入记忆不可见，不知道是否召回记忆，记忆召回是否正确。（记忆脑没有记忆召回，也没有记忆的精简信息插入）
5.记忆脑的四步分析需要支持异步，不要阻塞主流程。